name: test self hosted runner
run-name: 🚀
on:
  push:
    branches: [main]
jobs:
  task1:
    runs-on: label-1
    steps:
      - name: Print Hello World
      - shell: cmd
        run: |
          @echo on
          echo "Hello World" >> myFile.txt