name: test self hosted runner
run-name: 🚀
on:
  push:
    branches: [main]
jobs:
  task1:
    runs-on: label-1
    steps:
      - name: Print Hello World
        run: |
          ls -lah
          bash printHello.sh
